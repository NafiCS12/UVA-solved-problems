#include<stdio.h>
#include<math.h>

int main()
{
    int i,j,n,f,size,p,ani,e,sum=0;

    scanf("%d",&n);

    for(i=1;i<=n;i++)
    {
        sum = 0;
        scanf("%d",&f);

        while()
        {
            while(scanf("%d %d %d",&size,&ani,&e) !=EOF)

            p = size*e ;
            sum = sum + p ;

        }
            printf("%d\n",sum);
    }

    return 0;
}
