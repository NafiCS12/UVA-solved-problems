#include<stdio.h>
#include<string.h>
int main()
{
    char y,s,i,t,array[100];
    long long int X;

    scanf("%s",array[i]);
    //gets(array);
    printf("%s",array[i]);
    return 0;
}

