#include<stdio.h>
#include<string.h>

int main()
{
    int k[101],T,j,i;
    char a[10][101];

    scanf("%d",&T);

    for(i=0;i<T;i++)
    {
        k[0]=n;

        for(j=1;j<=10;j++)
        {
            scanf("%s",a[j]);
            scanf("%d",&k);

            if(k[j]>n)
            {
                n = k[j];
                c = c+1;
            }
        }

        for(e=0;e<c;e++)
        {
            printf("Case #%d:\n");
        }
    }

    return 0;
}
