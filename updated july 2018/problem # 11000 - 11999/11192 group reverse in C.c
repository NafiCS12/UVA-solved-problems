#include<stdio.h>
#include<string.h>

int main()
{
    int num,i,j,l;
    char inp[1001],outp[1001];
    while( scanf("%d",&num)==1)
    {
        if(num==0)
            break;
        else
        {
            gets(inp);
            l=strlen(inp);
            for(i=0;i<(i+num);i=i+num)
            {
                j = num-1;
                out[j]=inp[i];
                j--;
            }


        }
    }
}
